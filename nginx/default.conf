# ============================================================
# HOMELAB NGINX
# Reverse Proxy
# Version: 0.5.0-dev
# ============================================================


# WebSocket connection handling
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server_tokens off;

add_header X-Content-Type-Options "nosniff" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;


# ============================================================
# HOMELAB DASHBOARD
# ============================================================

# Docker/internal health endpoint
server {
    listen 80 default_server;
    server_name _;

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }

    location / {
        return 404;
    }
}


# LAN HTTP -> HTTPS
server {
    listen 80;

    server_name
        dashboard.home.arpa
        portainer.home.arpa
        kuma.home.arpa
        prometheus.home.arpa
        grafana.home.arpa;

    return 301 https://$host$request_uri;
}


# Local troubleshooting
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 443 ssl;
    server_name dashboard.home.arpa;

    ssl_certificate /etc/nginx/tls/homelab-server.crt;
    ssl_certificate_key /etc/nginx/tls/homelab-server.key;

    ssl_protocols TLSv1.2 TLSv1.3;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}


# ============================================================
# PORTAINER
# ============================================================

server {
    listen 443 ssl;
    server_name portainer.home.arpa;

    ssl_certificate /etc/nginx/tls/homelab-server.crt;
    ssl_certificate_key /etc/nginx/tls/homelab-server.key;

    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://portainer:9000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}


# ============================================================
# UPTIME KUMA
# ============================================================

server {
    listen 443 ssl;
    server_name kuma.home.arpa;

    ssl_certificate /etc/nginx/tls/homelab-server.crt;
    ssl_certificate_key /etc/nginx/tls/homelab-server.key;

    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://uptime-kuma:3001;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

# ============================================================
# PROMETHEUS
# ============================================================

server {
    listen 443 ssl;
    server_name prometheus.home.arpa;

    ssl_certificate /etc/nginx/tls/homelab-server.crt;
    ssl_certificate_key /etc/nginx/tls/homelab-server.key;

    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://prometheus:9090;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================================
# GRAFANA
# ============================================================

server {
    listen 443 ssl;
    server_name grafana.home.arpa;

    ssl_certificate /etc/nginx/tls/homelab-server.crt;
    ssl_certificate_key /etc/nginx/tls/homelab-server.key;

    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://grafana:3000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}